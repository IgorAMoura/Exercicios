// 10aça um programa que receba um número do usuário e continue
// a pedir o número se o numero for diferente de 0.

let numero = parseInt(prompt("insira"))

do{numero = parseInt(prompt("digite um numero: "))}
while(numero !== 0)

